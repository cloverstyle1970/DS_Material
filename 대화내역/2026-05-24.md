# 작업 대화 내역 — 2026-05-24

## 세션 개요

본 프로젝트 신규 코드 변경 없음. 별개 저장소(`daesol-sketch.github.io`) 클론해 구조만 살펴보고, 5/21 대화 내역 커밋·푸시, 개발 서버 기동.

---

## 1. 외부 저장소 클론 — `daesol-sketch.github.io`

`H:\DS_Material\daesol-sketch.github.io\` 위치에 `git clone https://github.com/daesol-sketch/daesol-sketch.github.io.git` 수행. 본 프로젝트와는 **별개 시스템**(승강기 유지보수 신고/배정 PWA, GitHub Pages 정적 배포).

### 폴더 구조 요약
```
daesol-sketch.github.io/
├── index.html         (2.94 MB)  # 전체 앱 (SPA, 단일 HTML 인라인)
├── manual.html        (187 KB)
├── manifest.json                 # PWA "대솔이엘 유지보수"
├── sw.js                         # Service Worker (캐시 v11 + 푸시 알림)
├── *.png                         # 로고/아이콘
└── supabase/
    ├── rls-setup.sql             # RLS 1회 세팅
    ├── migrations/20260424_add_assigned_at.sql
    └── functions/                # Edge Functions (Deno) 11개
        ├── admin-auth-ops
        ├── call-notify           # 현장 전화 착신 푸시
        ├── elevator-info
        ├── inspection-sync
        ├── migrate-to-auth       # accounts → Supabase Auth 이관
        ├── pending-feedback-notify
        ├── repeat-assign-notify
        ├── send-assign-notify
        ├── sync-elevator-models
        ├── tmap-geocode
        └── unack-assign-notify
```

### 주요 특징
- **빌드 시스템 없음** — 단일 HTML에 모든 로직 인라인, CDN으로 `xlsx`/`supabase-js`/`jspdf`/`exceljs`/`html2canvas` 로드.
- **백엔드**: Supabase 직접 호출 + Edge Function 11개. URL과 anon key가 `sw.js`에 하드코딩(`bbnmxwpacdfqvicybhau`).
- **DB 테이블** (rls-setup.sql 기준): `reports`, `accounts`, `managed_sites`, `duty_roster`, `holidays`, `push_subscriptions`, `site_elevators`.
- **인증 패턴**: `username` → `{uuid}@daesol.el` 가상 이메일 매핑(`get_user_auth_email` RPC, SECURITY DEFINER).
- **알림**: Web Push(Service Worker), 알림 타입 `call`/`feedback`/배정 통지.
- **다크/라이트 모드**: CSS 변수 토글(`body.light-mode`).

### 본 프로젝트와의 관계
참고용으로 클론만. 본 저장소에는 **커밋하지 않음** (사용자 결정: "그냥 무시").

---

## 2. 개발 서버 기동

- `npm run dev` 백그라운드 실행 (작업 ID `b6qshuk11`).
- Next.js 16.2.4 (Turbopack) — Local: http://localhost:3000, Network: http://10.5.0.2:3000
- `Ready in 1940ms`, 에러 없음. `.env.local` + `.env` 로드 완료.

---

## 3. 커밋 / 푸시

| 커밋 | 메시지 | 내용 |
|---|---|---|
| `6b35b8b` | `docs: 5/21 대화 내역 추가` | 5/21 대화내역 1파일 추가, 코드 변경 없음 |

`origin/master` 푸시 완료 (`67b4cf1..6b35b8b`).

---

## 4. 산출물

- 코드/스키마 변경 **없음**.
- 신규 파일: `대화내역/2026-05-21.md` (커밋 완료), 본 파일 `대화내역/2026-05-24.md`.
- Untracked (의도적 보존): `daesol-sketch.github.io/` 별개 저장소 클론본.
- 미배포 마이그레이션: 없음.
- 다음 액션 대기: 5/21에 제시한 A(전표 묶음 편집 검증) / B+C(알림 확장) 라인 미선택 상태 유지.
