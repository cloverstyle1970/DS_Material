# 작업 대화 내역 — 2026-05-30

## 세션 개요

**근무복·안전장구 출고 흐름 완성** + **금액 입력 천단위 통일** + **사원 마스터 일관성 회복(users→accounts)** + **categories 마스터 신설/시드**. 핵심 기능 모두 신DB 직접 적용으로 즉시 가동.

> ⚠️ 신DB는 외부 담당자 관리. anon 키로 DDL 불가 → 서울 pooler(`aws-1-ap-northeast-2.pooler.supabase.com:5432`, user `postgres.bbnmxwpacdfqvicybhau`) 직접 접속으로 적용. 구DB도 동일 pooler / user `postgres.gwgzzsoknjulwwsmubju`. 비밀번호는 환경변수만 사용, 파일·메모리 미저장.

---

## 1. 근무복·안전장구 출고 흐름

* **신청 처리 자동 출고** [UniformSafetyAdminClient.tsx](file:///h:/DS_Material/src/components/uniform-safety/UniformSafetyAdminClient.tsx): 신청→처리중/수령완료 첫 전환 시 `add_transaction` RPC로 출고 트랜잭션 자동 발생(라인별, 동일 batch_id). 사전 재고 검증으로 부족 시 RPC 호출 자체 차단. `site_name="[지급] {수령자명}"`, note에 신청번호/부서/사이즈 기록.
* **관리자 직접 불출 등록 탭** [UniformSafetyDirectIssueTab.tsx](file:///h:/DS_Material/src/components/uniform-safety/UniformSafetyDirectIssueTab.tsx) 신설:
  * **단일 등록 폼**: 구분(근무복/안전장구) + 수령자(accounts 재직자) + 품목/자재 2단 셀렉트 + 사이즈/수량 → 신청+처리+수령 한 번에 처리, 출고 RPC 호출.
  * **엑셀 일괄 업로드**: 컬럼 `수령자명/부서/구분/자재코드/사이즈/수량/비고` (별칭 다수 허용, null-safe 매칭). 사전 검증 미리보기 → 정상 행만 `(수령자+구분)` 그룹별 일괄 처리. 템플릿 다운로드 버튼 제공. ref로 input value 리셋해 동일 파일 재선택 가능.
* **신청 화면 분리** [UniformSafetyClient.tsx](file:///h:/DS_Material/src/components/uniform-safety/UniformSafetyClient.tsx): D9902(근무복)/D9903(안전장구) 별도 쿼리. 품목(소분류) → 자재 2단 셀렉트로 같은 소분류의 자재 다건 중 선택 가능(이전엔 첫 자재만 노출).

## 2. categories 마스터 신설 + 시드

* [migration-add-categories.sql](file:///h:/DS_Material/scripts/migration-add-categories.sql): `categories(level/code/label/major_code/mid_code)` 테이블, level별 partial unique index 3개(시드 `ON CONFLICT DO NOTHING` 호환), `updated_at` 자동 트리거, RLS `allow_all`. 신DB에 한 번도 생성된 적 없던 핵심 마스터 — 분류 관리 모달 INSERT 무력화 원인.
* [seed-categories-from-codelist.sql](file:///h:/DS_Material/scripts/seed-categories-from-codelist.sql): `ds-page/part code list.xlsx` 추출 결과 UPSERT 시드(대 12 / 중 32 / 소 237). materials 사용 중인 대분류 17·33·51·53·65·86 은 placeholder. 99 대분류 체계 재정의 반영(99|02 근무복 / 99|03 안전용품 / 99|04 소모품).

## 3. 사원 마스터 일관성 — users → accounts 일괄 치환

* **함정 재확인**: `users` 는 Supabase Auth 시스템 테이블, 실제 사원 마스터는 `accounts`(58건). AuthContext 외 7곳이 잘못된 `users` 를 호출해 테마 저장·푸시 토글·근무복 사이즈 동기화·직접 불출 매칭이 무력화돼 있었음.
* **일괄 치환**: `crew.ts`, `MyDashboardContent.tsx`, `PushNotificationToggle.tsx`, `SettingsContent.tsx`, `UniformSafetyClient.tsx`, `UniformSafetyAdminClient.tsx`, `UniformSafetyDirectIssueTab.tsx` 의 `from("users")` → `from("accounts")`. `accounts` 조회는 `.eq("status","재직")` 추가.
* **CLAUDE.md 갱신**: "사원/개인정보 모델" 섹션의 `users` 표기 5건 → `accounts` 로 정정 + "**PostgREST `users` 호출 금지**" 경고 추가.
* **데이터 정정**: 구DB users → 신DB accounts name 동기화(43건 — 이전 username↔name 매핑 오염 정상화). 박흥식 id=52(중복 사원) dept='상주팀' 보강 후 삭제(외래키 참조 0건 확인) — 동명이인 정리.

## 4. 금액 입력 천단위 콤마 통일

* [input-format.ts](file:///h:/DS_Material/src/lib/input-format.ts) 에 `formatMoney(value)` 신설.
* **적용 5건** — 모두 `type="text" inputMode="numeric" text-right` + 입력 시 `formatMoney`, 저장 시 `parseNum`:
  * [AddMaterialModal.tsx](file:///h:/DS_Material/src/components/materials/AddMaterialModal.tsx)·[EditMaterialModal.tsx](file:///h:/DS_Material/src/components/materials/EditMaterialModal.tsx)·[RegisterRepairModal.tsx](file:///h:/DS_Material/src/components/materials/RegisterRepairModal.tsx) — 구매단가/판매단가
  * [QuoteDetailClient.tsx](file:///h:/DS_Material/src/components/quotes/QuoteDetailClient.tsx) — 입금 등록 팝업 (인라인 replace → 공용 유틸 통일)
  * [PurchaseOrderModal.tsx](file:///h:/DS_Material/src/components/requests/PurchaseOrderModal.tsx) — 발주 모달 라인별 단가 (`type="number"` 잔재 발견 후 정정)
* **전수 점검 결과**: QuoteEntryClient·QuoteSettingsClient·LaborRatesClient(수정 모달)·PurchaseOrderEntry·InboundEntry 는 이미 공용 패턴 적용됨.

## 5. 공임단가 엑셀 다운로드

* [LaborRatesClient.tsx](file:///h:/DS_Material/src/components/quotes/LaborRatesClient.tsx): "📥 엑셀 다운로드" 버튼 추가. 현재 필터(활성/검색) 적용된 결과만 export. 시트 `공임단가`, 컬럼 8개(코드/공정명/분류/단위/단가/설명/활성/정렬). 파일명 `공임단가_YYYYMMDD.xlsx`.
* **추가 요청**: 외주비 기준 엑셀(`공임비 기준_211126(대솔).xlsx`) 검토 → 자사 직접 작업과 무관(외주)으로 사용자 결정, import 보류.

---

## 메모리 갱신

* **신규**: `architecture-accounts-is-user-master` (사원 정보=accounts 규정), `feedback-money-format-thousand-separator` (금액 천단위 규정), `reference-supabase-db-direct` (Postgres pooler 접속 endpoint).
* **갱신**: `project-categories-99-conflict` 를 "안정화"로 마무리, 안전장구 코드체계 변경 이력 반영.

## 변경 파일 (커밋 `217125b`)

* **신규** (3): `scripts/migration-add-categories.sql`, `scripts/seed-categories-from-codelist.sql`, `src/components/uniform-safety/UniformSafetyDirectIssueTab.tsx`
* **수정** (14): `.claude/claude.md`, `src/components/materials/{AddMaterialModal,EditMaterialModal,RegisterRepairModal}.tsx`, `src/components/me/{MyDashboardContent,PushNotificationToggle}.tsx`, `src/components/quotes/{LaborRatesClient,QuoteDetailClient}.tsx`, `src/components/requests/PurchaseOrderModal.tsx`, `src/components/settings/SettingsContent.tsx`, `src/components/uniform-safety/{UniformSafetyAdminClient,UniformSafetyClient}.tsx`, `src/lib/{crew,input-format}.ts`
* **신DB 직접 적용** (코드 변경 없음): categories 테이블+시드 적용, 자재명 정정 43건, 박흥식 dept 보강·삭제

## 검증

* `npx tsc --noEmit` 전 구간 통과. 사용자 브라우저 확인: 견적 작성 단가·발주서 단가 입력 모두 천단위 동작 정상.

---

## 추가 세션 — 공임단가/공수자료 검토 (보류 상태)

### 7. 외주비 기준 엑셀 검토 (`공임비 기준_211126(대솔).xlsx`)
* 49개 항목 (11개 대분류) — MAIN SHEAVE / GEARLESS / MAIN ROPE / MAIN SHEAVE&ROPE / WORM GEAR / TM BEARING / TM 교체 / CAR·CWT SHEAVE / DEFLECTOR SHEAVE / ROPE CUTTING / 모터베어링·가바나류
* 구버전(8컬럼)에는 정가+10% 감액 병기, 대솔 버전(7컬럼)은 정가만
* **사용자 결정: 외주 관련 항목은 무시 → import 보류** (자사 직접 작업 단가가 따로 필요하면 그때 재검토)

### 8. 교체공사 공수자료 분석 (`교체공사 공수자료_211126(대솔).xlsx`)
* 같은 49개 항목의 **공수만 발췌한 버전** (외주비 컬럼 제거, 컬럼 5개: 품명/TYPE/MR-MRL/세부사항/공수)
* 공수 분포: 3공 9건 / 4공 17건 / 5공 14건 / 6공 5건 / 7공 4건 / 미기재 4건 (모터베어링·가바나류 = "별도 견적")
* **현 시스템(`labor_rates`) 한계**: `unit_price` 1개 컬럼만 있어 "공수 × 1공 단가" 모델 미지원. 변형(TYPE·MR/MRL·층수)을 한 행에 풀어 적어야 함.

### 9. 검토해야 할 6가지 결정 사항 제시 → 사용자 답변 보류 중

제안한 기본 옵션:
1. **(a) DB 모델**: `labor_workload_standards` 표준 테이블 신설 — `id, code, category, type_name, subtype, floor_range, man_days, is_active, sort_order, note`
2. **(b) 미기재 4건**: `man_days=NULL` + `note="별도 견적"` 으로 포함
3. **(c) 변형 매핑**: 49개 행 단순 펼침
4. **(d) 견적 entry 연동**: "작업 라인 추가" 섹션 신설, 표준 선택 시 공수 자동, 1공 단가는 견적 헤더 값 사용
5. **(e) 외주/자사 구분**: `is_outsource_only` 플래그
6. **(f) 갱신 주기**: 자유 편집

단계 분리 권장: ① DB+import 먼저, ② 견적 entry 연동 별도 작업.

### 10. ⏳ 보류 상태 — 사용자 답변 모호

* 사용자 답변: **"4번으로 진행"**
* 두 가지 해석 가능:
  * A. 위 1~6번 **묶음 전체** 진행 (단계 ① → ②)
  * B. **(d) 견적 entry 연동만** 진행 → 단, (a)~(c) 선결 필요해 추가 질문 필요
  * C. 다른 의미 가능
* 한글 깨짐으로 AskUserQuestion 도구 사용 실패 → 텍스트로 재질문했으나 답변 대기 중
* **다음 세션에서 "이어서 작업해줘" 입력 시 위 A/B/C 옵션을 다시 텍스트로 제시할 것** — 메모: [[next-task-workload-standards]] 참조
