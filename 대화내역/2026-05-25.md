# 작업 대화 내역 — 2026-05-25

## 세션 개요

dev 서버 기동 후 **도움말센터(Manual Center)에 PDF 매뉴얼 등록 기능 추가**. 기존 마크다운 본문 방식에 더해 PDF 파일을 업로드해 매뉴얼로 등록·열람할 수 있도록 확장. 커밋·푸시 1건 완료.

---

## 1. dev 서버 기동

- `npm run dev` 백그라운드 (Next.js 16.2.4 Turbopack, `Ready in 1934ms`).
- Local `http://localhost:3000` 브라우저 오픈.

---

## 2. 도움말센터 PDF 매뉴얼 등록 기능

### 2-1. 기존 구조 파악
- `manuals` 테이블: `id, category, title, content(markdown), sort_order, updated_at, updated_by`.
- `src/components/manual/ManualCenterClient.tsx` — 마크다운 본문 저장/렌더, 관리자 추가·수정·삭제, 정적 폴백(`STATIC_FALLBACK_MANUALS` 3건) + DB 모드 분기.
- 파일 업로드 패턴: 다른 컴포넌트들이 `supabase.storage.from(BUCKET).upload()` + `getPublicUrl()` 직접 호출. 버킷은 `migration-*.sql`의 `INSERT INTO storage.buckets`로 정의 (예: `cert-docs`, `material-opinions`, `tbm-photos`).

### 2-2. 마이그레이션 (신규)
`scripts/migration-add-manual-pdf.sql`:
- `ALTER TABLE manuals ADD COLUMN IF NOT EXISTS pdf_url TEXT;`
- `ALTER TABLE manuals ALTER COLUMN content DROP NOT NULL;` (PDF 매뉴얼은 본문 없음)
- Storage 버킷 `manual-docs` 생성 (public, 50MB, `application/pdf`만) + upload/read/delete 정책.
- `scripts/build-migration-all.mjs` ORDER에 `migration-add-manuals.sql` 다음 위치로 등록 → `migration-all.sql` 재빌드 (34개 결합).

### 2-3. UI 변경 (`ManualCenterClient.tsx`)
- `ManualRecord`에 `pdf_url: string | null` 추가, 폴백 3건에 `pdf_url: null` 추가.
- 상수 `MANUAL_DOCS_BUCKET = "manual-docs"`, 헬퍼 `uploadPdf(file)` 추가.
- 편집 상태 `editType("markdown"|"pdf")`, `editPdfFile`, `editPdfUrl` 추가. `startCreate`/`startEdit`에서 초기화 (편집 시 `pdf_url` 유무로 유형 판별).
- 작성 폼: **유형 토글**(`📝 문서 직접 작성` / `📕 PDF 파일 등록`). PDF 선택 시 파일 업로더 표시, 마크다운 선택 시 기존 textarea.
- `handleSave`: 유형별 검증 분기. PDF면 신규 파일 업로드 후 `pdf_url` 저장(`content=""`), 마크다운이면 `pdf_url=null`. payload 공통화.
- 상세 뷰: `pdf_url` 있으면 `<iframe>` 내장 뷰어 + "새 탭에서 열기" 링크, 없으면 기존 `MarkdownRenderer`.
- 상단 버튼: PDF면 `📥 PDF 열기/다운로드`(a 태그, iframe은 `window.print()` 부정확), 마크다운이면 기존 `🖨️ PDF 출력/저장`.
- 사이드바 목차 아이콘: PDF `📕` / 문서 `📄` 구분.

### 2-4. 검증
- `npx tsc --noEmit -p .` 통과.
- dev 로그상 `/manual` 200 응답 정상 컴파일.
- ⚠️ **현 시점 Supabase에 `manuals` 테이블 미생성** (`PGRST205` → 정적 오프라인 모드 동작 중). 실제 동작하려면 SQL Editor에서 `migration-add-manuals.sql` → `migration-add-manual-pdf.sql` (또는 `migration-all.sql`) 실행 필요.

---

## 3. 커밋 & 푸시

- 관련 4파일만 스테이징 (무관한 untracked `daesol-sketch.github.io/` 제외).
- 1차 커밋 시 Bash 툴에 PowerShell here-string(`@'...'@`) 오용 → 메시지에 `@` 끼임 → `--amend`로 정정.
- 커밋 `2c239fc` `feat(manual): 도움말센터 PDF 매뉴얼 등록 지원` → `origin/master` 푸시 완료 (9ef530c..2c239fc).

---

## 다음 작업 후보

- Supabase에 manuals 관련 마이그레이션 실제 실행 (현재 미적용).
- PDF 매뉴얼에도 선택적 설명(content) 동시 첨부 허용 여부 검토 (현재 PDF는 본문 빈 문자열).

---
---

# 작업 대화 내역 — 2026-05-25 (세션 2)

## 세션 개요

**TK 자재 색상 구분 → 발주서/거래명세서 인쇄 양식 신설 → 회사 인감도장 등록·표시 → DS 자재 품명·규격 일괄 갱신.** 커밋·푸시 3건(`b561644`, `8d02e97`, `090baa0`) + 자재 데이터 375건 업데이트.

---

## 1. TK 자재 자재명·코드 파란색 표시 (커밋 b561644)

- 공통 헬퍼 `src/lib/material-style.ts` 신설: `isTkMaterial(id)`(코드 첫 글자 ≠ 'D'), `TK_TEXT_CLASS`(화면 `text-blue-600 dark:text-blue-400`), `tkPrintTextClass`(인쇄 `text-blue-700`).
- 화면 적용: 자재관리·재고실사(수량/S/N)·자재신청/발주·입출고이력 — 자재명/코드 셀 `isTkMaterial(id) ? TK : 기존색`.
- 전표 입력 적용: 발주·입고·출고 그리드 input(`!text-blue-600`로 cellInput override), 자재 검색 드롭다운, 참조 팝업, 재고조정 모달. `MatInlineSearch`에 `materialId` prop 추가.
- 출력 적용: 견적서·거래명세서·세금계산서 품목명.
- `globals.css`에 `@media print { print-color-adjust: exact }`.
- 미적용: 대시보드 "최근 자재 신청"(`RecentRequest`에 자재코드 없음).

## 2. 전표 버튼 정리 + 발주서 인쇄 양식 (커밋 8d02e97)

- 발주/입고/출고 저장 버튼 라벨에서 F8/F7 표기 제거, 입고 "저장/전표" 버튼 삭제.
- 발주서 인쇄 양식 신설 `PurchaseOrderPrintPaper.tsx`: A4(210×297mm), 품목 최소 20행(빈 행 패딩), 품목코드 컬럼 제거·단가/공급가액 공란, 발주자·발송지 기록란, 상단 여백 확대, 하단 문구 제거.
- "발주서" 버튼/F7 → 미리보기 오버레이 → 인쇄 (신규·수정 모드 모두 노출).
- 발송지: 입력 검색(지정 5곳 상위 + 현장, 선택 시 주소 자동). 지정 5곳 = 대산화물(고양식사)/경동화물(장항543·파주아동동353)/당사 사무실(택배·지하2층).
- 발송지·담당자 등 발주 데이터 저장: `purchase_orders` ship_* 6컬럼 (`migration-add-po-ship-info.sql`). 담당자=신청자, 연락처=신청자 전화 자동.
- 발주자 = 발주서 작성자(로그인 사용자).

## 3. 출고 거래명세서 (거래명세서-대솔.xlsx 기준)

- `OutboundDeliveryNotePaper.tsx` 신설: 상하 2부(공급자 보관용=파랑 / 공급받는자 보관용=빨강), 테두리·항목명 색상, 입력값·데이터는 검정.
- 공급자(대솔) 정보 고정. 공급받는자 상호 = 거래처 검색 선택 → 등록번호·주소·전화 자동(vendors). 품목 년/월/일·공급가액·세액(10%)·합계 자동.
- 출고전표 "거래명세서" 버튼/F7 → 미리보기 → 인쇄.
- ⏳ 미해결: 거래처 등록번호/주소 자동표시 안 되는 사례 보고 → 메모리 `dn-vendor-address-issue` + 코드 `TODO(추후 검토)` 기록.

## 4. 회사 인감도장·로고 (커밋 090baa0)

- `migration-add-company-assets.sql`: `quote_settings`에 `company_stamp_url`·`company_logo_url` 컬럼 + `company-assets` Storage 버킷.
- 회사정보 관리(`CompanyInfoClient`)에 인감/로고 이미지 업로드(미리보기·제거) 추가.
- 인감 표시: 견적서·발주서 대표이사, 출고 거래명세서 공급자 상호 칸, 거래명세서/세금계산서 대표이사·대표자. (납품자 칸 공란)

## 5. DS 자재 품명·규격 일괄 갱신

- `ds자재 업데이트 자료_260525.xlsx`(1944건) 기준, 자재코드로 `materials.name`/`model_no`만 갱신(단가·재고·보관장소 보존).
- 스크립트 `scripts/update-material-names.mjs` (옵션 없으면 미리보기, `--apply`로 적용).
- 결과: 375건 변경(대부분 `[DF]`·`[Delete]` 접두 제거, `LOP→LIOP` 정정 등), 1569건 동일, DB 미존재 0건. 적용 후 재검증 변경 0건.

---

## ⚠️ 미적용 마이그레이션 (Supabase SQL Editor 수동 실행 필요)

- `migration-add-po-ship-info.sql` — 발주 발송지 저장 (미실행 시 발주 저장 실패)
- `migration-add-company-assets.sql` — 인감/로고 (미실행 시 회사정보 화면 `company_stamp_url does not exist` 에러)

## 다음 작업 후보

- 거래처 등록번호/주소 자동표시 이슈 재검토 (현장 ↔ 거래처 매핑 설계).
- 회사 로고 이미지 문서 활용 (현재 등록만, 출력물 미사용).
- `scripts/update-material-names.mjs` 커밋 여부 결정.
